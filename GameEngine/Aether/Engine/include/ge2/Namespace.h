#pragma once

// Transitional namespace: the engine was originally authored under `Aether::`.
// New code should prefer `ge2::` APIs. For now `ge2::` is an alias to `Aether::`
// so the project can migrate incrementally without breaking existing code.

namespace Aether {}

namespace ge2 = ::Aether;

